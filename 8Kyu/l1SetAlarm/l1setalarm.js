//My solution
function setAlarm(employed, vacation){
    return employed && !vacation ? true : false
  }

//Solved with fat arrow function
//const setAlarm = (employed, vacation) => employed && !vacation;